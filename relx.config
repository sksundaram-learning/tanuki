{paths, ["apps", "deps"]}.
{default_release, tanuki, "0.1.0"}.

%% comment this line for a release that ships its own Erlang VM
{include_erts, false}.
%% uncomment this line to ship a release without the source code included
% {include_src, false}.

{release, {tanuki, "0.1.0"}, [
    {tanuki_backend,  "0.1.0"},
    {tanuki_incoming, "0.1.0"}
]}.

{sys_config, "./config/sys.config"}.
{vm_args, "./config/vm.args"}.
