{erl_opts, [debug_info, fail_on_warning]}.

{deps, [
    {couchbeam, {git, "https://github.com/benoitc/couchbeam", {tag, "1.3.1"}}}
]}.

%
% $ cd apps/tag_to_location
% $ rebar3 escriptize
% $ ./_build/default/bin/tag_to_location <config> <tag>
%
{escript_name, tag_to_location}.
{escript_incl_apps, [
    couchbeam,
    hackney,
    idna,
    jsx,
    ssl_verify_fun
]}.
