{erl_opts, [debug_info, fail_on_warning]}.

{deps, [
    {couchbeam,  {git, "https://github.com/benoitc/couchbeam",    {tag, "1.3.1"}}},
    {emagick_rs, {git, "https://github.com/nlfiedler/emagick.rs", {tag, "0.4.1"}}}
]}.

%
% $ cd apps/correct_orientation
% $ rebar3 escriptize
% $ NIF_DIR=_build/default/lib/emagick_rs/priv ./_build/default/bin/correct_orientation ...
%
{escript_name, correct_orientation}.
{escript_incl_apps, [
    couchbeam,
    emagick_rs,
    hackney,
    idna,
    jsx,
    ssl_verify_fun
]}.
