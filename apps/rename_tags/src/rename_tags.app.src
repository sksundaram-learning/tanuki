{application, rename_tags, [
    {description, "Rename a tag in all matching documents."},
    {vsn, "1.0.0"},
    {registered, []},
    {applications, [
        kernel,
        stdlib,
        couchbeam,
        hackney,
        idna,
        jsx,
        ssl_verify_hostname
    ]},
    {mod, {rename_tags, []}},
    {env, []}
]}.
